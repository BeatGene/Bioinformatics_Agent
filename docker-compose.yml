services:
  app:
    build: .
    container_name: bioinformatics-agent
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=7860
    volumes:
      - ./data:/app/data
    restart: unless-stopped
